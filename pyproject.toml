[project]
name = "fig"
version = "0.1.0"
description = "Fragrance information grabber and visualizer."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "streamlit>=1.38.0",
  "pandas>=2.2.2",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
